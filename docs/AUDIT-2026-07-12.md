# Audit di presa in carico — 2026-07-12

Intervento di consolidamento della base tecnica (stabilità, sicurezza,
documentazione, ambienti). Questo file è la fotografia dello stato trovato,
di cosa è stato fatto e di cosa resta. Punto di ripristino:
tag git `snapshot-2026-07-12-consolidamento` (= main `aee777c`, v1.4.4).

## 1. Stato iniziale trovato

**In salute (meglio del previsto):**
- **Parità prod↔repo perfetta**: hosting (327/327 file MD5 identici),
  Cloud Functions (117 default + 3 publicprofile, elenchi identici al
  source), Firestore rules byte-identiche a `main`. Nessun "Frankenstein".
- **Test solidi e verdi**: 416 unit + 120 rules (ora 418+121), e2e
  Playwright su emulatori funzionante, CI esistente (solo rules).
- **Sicurezza**: zero findings critici/alti. Rules senza `allow write: if
  true`; callable autenticate salvo le 10 pubbliche by-design; rate
  limiting diffuso; niente segreti nel repo; storage rules con limiti.
- **Guardrail deploy già progettati** (`check-deploy-safety.mjs`,
  `deploy:staging`/`deploy:prod` con `CONFIRM_PROD`) — mancava solo il
  progetto staging reale.
- **PWA quasi Android-ready**: manifest core ok, SW con offline fallback,
  pagine delete-account/privacy/terms live.

**Problemi trovati:**
| Gravità | Problema | Esito |
|---|---|---|
| Importante | Nessun ambiente staging: si provava in prod | **RISOLTO** — creato `somto-staging` |
| Importante | Indice Firestore live (`titles status+ratingCount`, usato da `listPopularTitles`) NON tracciato in `firestore.indexes.json` → un deploy indexes l'avrebbe cancellato rompendo la query | **RISOLTO** — aggiunto al file |
| Importante | CLAUDE.md 122KB (quasi tutto log storico): costoso e fragile per ogni agente futuro | **RISOLTO** — 44KB + `docs/RELEASE_HISTORY.md` |
| Importante | 5 appunti personali (docx/analisi) tracciati e pushati su origin | **RISOLTO** — fuori da git (history NON riscritta: repo privato, force-push vietato) |
| Importante | 5 branch remoti morti (tutti già mergiati, verificato via patch-id/ancestry) + 2 stash superati + worktree orfana | **RISOLTO** — eliminati, hash in DECISIONS.md |
| Importante | Notifica `official_update` con link morto (`/home.html?post=` mai gestito) | **RISOLTO** — live (deploy functions 2026-07-12) |
| Media | CI su Node 20 vs engines 22; unit e rules nello stesso job; niente build blog | **RISOLTO** — `.github/workflows/ci.yml` |
| Media | Sistema editoriale senza UI, senza ritiro, bozze illeggibili | **RISOLTO** — live (console + unpublish, 2026-07-12) |
| Media | `getGuestQuiz`/`submitGuestQuiz` senza rate limit | **RISOLTO** — live (deploy functions 2026-07-12) |
| Media | Vulnerabilità transitive high/critical (firebase-admin/google-gax) nei 3 package | **RISOLTO** — bump dipendenze, test verdi |
| Media | CSP solo Report-Only con `unsafe-inline` | **RIMANDATO** — richiede refactor inline-script su ~60 pagine; enforcing ora romperebbe la PWA. Tracciato in SECURITY.md |
| Bassa | Ramo morto `request.auth.token.admin` in storage.rules (custom claim mai settato, irraggiungibile, fail-safe) | **DOCUMENTATO** in SECURITY.md — rimozione = deploy storage rules, non urgente |
| Bassa | `docs/E2E_TESTS.md` con path assoluti di un vecchio checkout | **RISOLTO** |
| Bassa | 44MB di screenshot App Store tracciati in docs/SOMTO_GO_LIVE/step_4 | **RIMANDATO** — rimuoverli richiede rewrite della history (vietato); solo costo clone, nessun rischio |
| Bassa | Branch protection GitHub non attivabile (repo privato, piano Free) | **DOCUMENTATO** — mitigato da guardia deploy + CI; decisione (Pro/pubblico) al proprietario |

**Non problemi (verificati e scartati):**
- App Store fermo a 1.3.2 con TestFlight a 1.4.4: stato intenzionale
  (1.3.3→1.4.4 mai sottomesse), non una regressione.
- Firestore come database: modello sano, denormalizzazioni intenzionali e
  mantenute da trigger + reconcile. Nessuna ragione per migrare a SQL.

## 2. Interventi effettuati (commit su main, 2026-07-12)

1. `569bf7e` staging somto-staging + firebaseConfig per hostname
2. `235dbf0` indice titles status+ratingCount tracciato
3. `35919c9` appunti privati fuori da git, mockup design dentro
4. `1911ddf` CI: unit+rules+blog, Node 22
5. `ea5cfb3` editoriale: unpublishOfficialUpdate + rules admin-read + fix ctaUrl
6. `0a88703` base documentale (11 doc) + CLAUDE.md snello
7. `c62abff` manifest PWA lang/categories
8. (successivi: console admin editoriale, rate limit guest quiz, bump
   dipendenze — vedi git log)

**Deploy**: prima fase solo staging; poi, su autorizzazione dell'utente,
anche prod (vedi §3).

## 3. Deploy prod — ESEGUITI il 2026-07-12 (autorizzati dall'utente)

1. ✅ `firestore:rules` (blocchi officialUpdates admin-read + guestRateLimits deny-all).
2. ✅ 4 functions: `publishOfficialUpdate` (fix ctaUrl), `unpublishOfficialUpdate`
   (nuova), `getGuestQuiz`/`submitGuestQuiz` (rate limit per-IP).
3. ✅ hosting con SW `v109-2026-07-12-editorial-console` (console admin
   editoriale, manifest lang/categories, badge Ufficiale, firebaseConfig).
Verifica live: SW v109 su somto.it, /admin-official-updates.html → 200.
Inoltre: Auth staging Email/password abilitata + utente QA creato e login
verificato (somto-staging).

## 4. Interventi rimandati (con motivazione)

- **CSP enforcing**: serve rimuovere `unsafe-inline` (nonce/hash su tutte le
  pagine) — progetto a sé, alto rischio di rompere la PWA per un guadagno
  incrementale (gli altri header sono già enforced).
- **ESLint**: zero config esistente; introdurlo ora = migliaia di warning
  senza valore. Vedi DECISIONS.md.
- **Blaze su staging** (functions/storage di staging): decisione di
  fatturazione del proprietario.
- **Storia git con i 5 file privati**: rimuoverli dal passato richiede
  `git filter-repo` + force push (vietato dalle regole repo). Il repo è
  privato: rischio accettato e documentato.
- **Scheduling editoriale**: design pronto in EDITORIAL_SYSTEM.md, non
  implementato (MVP prima).
- **Sweep code-quality profondo su public/js + iOS**: il repo ha già avuto
  audit recenti (iOS 2026-06-19, web 2026-06-19, sicurezza 2026-06-14) i cui
  fix sono in prod; un nuovo sweep generalizzato avrebbe costo alto e
  rendimento incerto. Priorità data a staging/CI/doc/editoriale.

## 5. Rischi residui noti

1. **Ondate di costo Firestore** legate agli import (mitigazioni live dal
   2026-07-11, monitorare — procedura in RUNBOOK.md).
2. **Sessioni AI parallele sullo stesso checkout**: pattern storico di
   incidenti (commit interlacciati, deploy da tree sporco). Le guardie
   deploy aiutano; la disciplina in CLAUDE.md resta necessaria.
3. **Coorte iOS vecchie**: la rule compat per la watchlist va rimossa solo
  quando la coorte App Store ≥1.3.0 (vedi memoria progetto).
4. **Nessuna protezione branch server-side**: un push forzato o un commit
   diretto sbagliato su main resta tecnicamente possibile.

## 6. Come riprendere il lavoro (per il prossimo agente/dev)

Leggere in ordine: `README.md` → `CLAUDE.md` → questo file → il doc
specifico del task (tabella in README). Lo stato "cosa resta da fare" vive
in: CLAUDE.md → *Pending residui* (aggiornato), qui → §3 (deploy eseguiti)
e §4 (rimandati).
