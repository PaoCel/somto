/**
 * tabbar.js — shim di compatibilità.
 * La shell condivisa (header + tabbar + menu + ricerca) vive in
 * js/components/appShell.js. Questo file resta per i vecchi import.
 */
export { initAppShell, initTabbar } from "../components/appShell.js";
